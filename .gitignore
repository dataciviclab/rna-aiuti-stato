# Dati raw (XML) — troppi grandi, solo streaming
data/raw/*.xml

# Parquet derivati — generati, non in git (eccetto manifest)
data/derived/*
!data/derived/MANIFEST.json
!data/derived/manifests/*.json

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
